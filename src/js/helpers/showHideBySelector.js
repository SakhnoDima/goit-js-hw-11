
export function showSelector(rootSelector) {
rootSelector.classList.remove("visually-hidden"); // показал лоадер
}
export function hideSelector(rootSelector) {
rootSelector.classList.add("visually-hidden"); // спрятал лоадер
}

